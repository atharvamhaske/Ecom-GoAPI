---
title: 'Quickstart'
description: 'Get started with the E-commerce API in minutes'
icon: 'rocket'
---

## Setup Your API

<Steps>
  <Step title="Clone the repository">
    ```bash
    git clone https://github.com/atharvamhaske/Ecom-GoAPI.git
    cd Ecom-GoAPI
    ```
  </Step>

  <Step title="Start with Docker Compose">
    ```bash
    docker-compose up -d
    ```
    This starts both the PostgreSQL database and applies migrations.
  </Step>

  <Step title="Run the API server">
    ```bash
    go run cmd/*.go
    ```
    Server starts on `http://localhost:8080`
  </Step>
</Steps>

## Test the API

Verify the API is running:

```bash
curl http://localhost:8080/health
```

You should see: `root.`

## Your First API Call

Let's make your first API call to fetch products:

<CodeGroup>

```bash cURL
curl http://localhost:8080/api/v1/products
```

```javascript JavaScript
fetch('http://localhost:8080/api/v1/products')
  .then(response => response.json())
  .then(data => console.log(data));
```

```python Python
import requests

response = requests.get('http://localhost:8080/api/v1/products')
print(response.json())
```

```go Go
package main

import (
    "encoding/json"
    "net/http"
    "fmt"
)

func main() {
    resp, err := http.Get("http://localhost:8080/api/v1/products")
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    var products []map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&products)
    fmt.Println(products)
}
```

</CodeGroup>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Test API Endpoints"
    icon="play"
    href="/api-reference/introduction"
  >
    Try out the interactive API playground
  </Card>
  <Card
    title="View OpenAPI Spec"
    icon="file-code"
    href="https://github.com/atharvamhaske/Ecom-GoAPI/blob/main/openapi.yaml"
  >
    See the full OpenAPI specification
  </Card>
</CardGroup>

<Check>
  **You're all set!** Start testing your API endpoints in the API Reference section.
</Check>
